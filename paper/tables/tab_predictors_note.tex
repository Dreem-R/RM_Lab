{\footnotesize Odds ratios from the $L_2$-regularised logistic regression (standardised numeric predictors); SHAP rank is the position in the mean $|$SHAP$|$ ordering of the LightGBM model (`--' = outside the top 15).}
